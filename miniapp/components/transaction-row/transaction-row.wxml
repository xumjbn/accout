<!-- 账单行组件 - 对应 iOS TransactionRow -->
<view class="tx-row">
  <category-icon icon="{{icon}}" bgColor="{{color}}" size="64" />
  <view class="tx-info">
    <text class="tx-note">{{note || category}}</text>
    <view class="tx-meta">
      <text class="tx-category">{{category}}</text>
      <image wx:if="{{sourceIcon}}" class="tx-source" src="{{sourceIcon}}" mode="aspectFit" />
    </view>
  </view>
  <view class="tx-amount {{isExpense ? 'tx-expense' : 'tx-income'}}">
    {{isExpense ? '-' : '+'}}{{amountStr}}
  </view>
</view>
