.page {
  padding: 24rpx;
  min-height: 100vh;
}

.card {
  background: #fff;
  border-radius: 16rpx;
  padding: 24rpx;
  margin-bottom: 24rpx;
}

.type-toggle {
  display: flex;
  background: #F4F5FB;
  border-radius: 20rpx;
  margin-bottom: 24rpx;
  overflow: hidden;
}

.type-option {
  flex: 1;
  text-align: center;
  padding: 16rpx;
  font-size: 28rpx;
  color: #6B7280;
}

.type-option.active {
  background: #07C160;
  color: #fff;
}

.field {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 0;
}

.field-label {
  font-size: 28rpx;
  color: #6B7280;
  flex-shrink: 0;
  margin-right: 24rpx;
}

.field-input {
  flex: 1;
  text-align: right;
  font-size: 28rpx;
}

.money-input {
  font-size: 48rpx;
  font-weight: 600;
  font-variant-numeric: tabular-nums;
}

.picker-value {
  font-size: 28rpx;
  color: #111827;
}

.field-divider {
  height: 1rpx;
  background: #F4F5FB;
}

.save-btn {
  background: #07C160;
  color: #fff;
  text-align: center;
  padding: 24rpx;
  border-radius: 16rpx;
  font-size: 32rpx;
  font-weight: 600;
}

.save-btn.disabled {
  opacity: 0.4;
}

/* 分类值：图标 + 名称 */
.cat-value { display: flex; align-items: center; justify-content: flex-end; gap: 12rpx; }
.cat-value-circ { width: 48rpx; height: 48rpx; border-radius: 50%; display: flex; align-items: center; justify-content: center; }
.cat-value-img { width: 26rpx; height: 26rpx; }
